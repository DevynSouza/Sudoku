Test Plan::

Call program and provide test.txt as a command line argument
    ./myapp test.txt

Test the mark function by markng 1,1 with a value
Input 7 while in the menu to test the program functions
    This will call the test function which will print out the clusters before marking
    Then make a mark at [1,3].
    Then again it will print the clusters which can be used to compare and verify that shoop is working.

    Lastly, 6 is pressed in the menu to exit the program. 